<Window x:Class="EasySaveGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EasySave v2.0" Height="620" Width="950">
    <StackPanel Margin="15">

        <TextBlock Text="EasySave — Travaux de sauvegarde" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Formulaire création -->
        <GroupBox Header="Nouveau travail" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom :" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtName" Margin="0,5,10,5" TextChanged="txtName_TextChanged"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Type :" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <ComboBox Grid.Row="0" Grid.Column="3" x:Name="cmbType" Margin="0,5,0,5">
                    <ComboBoxItem Content="Complète" IsSelected="True"/>
                    <ComboBoxItem Content="Différentielle"/>
                </ComboBox>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Source :" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtSource" Margin="0,5,10,5"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Cible :" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox Grid.Row="1" Grid.Column="3" x:Name="txtTarget" Margin="0,5,0,5"/>

                <Button Grid.Row="2" Grid.Column="3" Content="Ajouter le travail" Click="AddJob_Click"
                        Margin="0,5,0,0" HorizontalAlignment="Right" Padding="10,4"/>
            </Grid>
        </GroupBox>

        <!-- Liste des travaux -->
        <DataGrid ItemsSource="{Binding Jobs}" AutoGenerateColumns="False"
                  Height="250" IsReadOnly="True" Margin="0,0,0,10"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="2*"/>
                <DataGridTextColumn Header="Cible" Binding="{Binding TargetPath}" Width="2*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                <DataGridTemplateColumn Header="Progression" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <ProgressBar Value="{Binding Progress}" Maximum="100" Height="14" Margin="2,2,2,1"/>
                                <TextBlock Text="{Binding StatusText}" FontSize="10" Margin="2,0,2,2" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Exécuter" Click="ExecuteJob_Click" Tag="{Binding Id}" Margin="2" Padding="5,2"/>
                                <Button Content="Supprimer" Click="DeleteJob_Click" Tag="{Binding Id}" Margin="2" Padding="5,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bouton principal -->
        <Button Content="Exécuter tous les travaux" Click="ExecuteAll_Click" Height="35" FontSize="14"/>

    </StackPanel>
</Window>
